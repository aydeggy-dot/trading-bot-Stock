package com.ngxbot.config;

import org.springframework.context.annotation.Configuration;

/**
 * Playwright configuration placeholder.
 * Browser beans will be created in Phase 7 when Meritrade automation is implemented.
 * Keeping this class as a marker for the Playwright-Java integration.
 */
@Configuration
public class PlaywrightConfig {
    // Playwright Browser bean creation deferred to Phase 7
    // to avoid requiring Chromium installed for compilation/testing
}
